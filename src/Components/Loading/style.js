import Styled from 'styled-components';

const StyledLoading = Styled.div `
  display: flex;
  flex-direction: row;
  width: 960px;
  height: 100px;
  margin: 0 auto;
`;

export default StyledLoading;
